{
  "name": "SpaceDuck",
  "version": "1.0.0",
  "description": "",
  "main": "webapp/server.js",
  "scripts": {
    "start": "cd webapp && node server.js",
    "dev": "cd webapp && nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "private": true,
  "dependencies": {
    "axios": "^1.6.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "better-sqlite3": "^9.4.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.7"
  }
}
